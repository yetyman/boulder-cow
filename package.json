{
  "name": "bouldercow",
  "scripts": {
    "dev": "concurrently \"cd backend && mvn spring-boot:run\" \"cd frontend && npm run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}